apiVersion: v1
kind: Secret
metadata:
  name: {{ include "comet-prelogin.fullname" . }}-image-pull-secret
  labels:
    {{- include "comet-prelogin.labels" . | nindent 8 }}
data:
  .dockerconfigjson: {{ required "Missing image.pullSecret" .Values.image.pullSecret }}
type: kubernetes.io/dockerconfigjson
