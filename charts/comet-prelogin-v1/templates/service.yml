apiVersion: v1
kind: Service
metadata:
  name: {{ include "comet-prelogin.fullname" . }}
  labels:
    {{- include "comet-prelogin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "comet-prelogin.selectorLabels" . | nindent 4 }}
